type Draft {
  id: ID! @unique
  createdAt: DateTime!
  updatedAt: DateTime!
  text: String!
  author: User
}

type User {
  id: ID! @unique
  email: String! @unique
  password: String!
  username: String! @unique
  posts: [Draft!]!
}